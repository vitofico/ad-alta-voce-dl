[project]
name = "ad-alta-voce-dl"
version = "0.3.0"
description = "RAI Ad Alta Voce audiobook downloader with periodic poller"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "tqdm",
    "flask",
    "mutagen",
    "schedule",
]

[project.scripts]
rai-dl = "rai.cli:main"
